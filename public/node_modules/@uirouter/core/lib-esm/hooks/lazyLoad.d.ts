import { Transition } from '@uirouter/core/lib-esm/transition/transition';
import { TransitionService } from '@uirouter/core/lib-esm/transition/transitionService';
import { StateDeclaration, LazyLoadResult } from '@uirouter/core/lib-esm/state/interface';
export declare const registerLazyLoadHook: (transitionService: TransitionService) => Function;
/**
 * Invokes a state's lazy load function
 *
 * @param transition a Transition context
 * @param state the state to lazy load
 * @returns A promise for the lazy load result
 */
export declare function lazyLoadState(transition: Transition, state: StateDeclaration): Promise<LazyLoadResult>;
