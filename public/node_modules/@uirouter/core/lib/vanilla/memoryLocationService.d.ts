import { BaseLocationServices } from '@uirouter/core/lib/vanilla/baseLocationService';
import { UIRouter } from '@uirouter/core/lib/router';
/** A `LocationServices` that gets/sets the current location from an in-memory object */
export declare class MemoryLocationService extends BaseLocationServices {
    _url: string;
    constructor(router: UIRouter);
    _get(): string;
    _set(state: any, title: string, url: string, replace: boolean): void;
}
